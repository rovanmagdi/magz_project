import { styled } from "@mui/material/styles";
import { Box, Button, Grid, useMediaQuery, useTheme } from "@mui/material";

export const StyledImageComment = styled(Box)(({ theme }) => ({
    height:"50px ",
    width:"50px",
    borderRadius:"50%"
  }));